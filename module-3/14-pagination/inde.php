global $conn;

mysql "LIMIT 100 OFFSET 100" skip 100 records
$gap = false;
$window= 1;
for pagination <li>
    if($i>1+$window && $i <$number_of_pages && abs($i - $current_page)> $window){

        }
</li>

$per_page = $_POST['number-of-results'] ?? $_GET['number-of-results'] ?? 12;